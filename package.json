{
  "name": "abdl-space-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
